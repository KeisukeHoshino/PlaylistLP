{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint:style": "stylelint 'resources/**/*.{css,scss}'",
    "lint:style:fix": "stylelint --fix 'resources/*.{css,scss}'",
    "lint": "npm run --silent lint:style; npm run --silent lint:es",
    "lint:fix": "npm run --silent lint:style:fix; npm run --silent lint:es:fix",
    "format": "prettier --write --loglevel=warn 'resources/**/*.{js,php,json}'",
    "fix": "sail npm run --silent format; npm run --silent lint:fix"
  },
  "devDependencies": {
    "axios": "^1.1.2",
    "laravel-vite-plugin": "^0.8.0",
    "prettier": "^3.1.0",
    "sass": "^1.66.1",
    "stylelint": "^15.11.0",
    "stylelint-config-prettier-scss": "^1.0.0",
    "stylelint-config-recess-order": "^4.4.0",
    "stylelint-config-standard": "^34.0.0",
    "stylelint-config-standard-scss": "^11.1.0",
    "stylelint-order": "^6.0.3",
    "vite": "^4.0.0"
  }
}
